# dbt package dependencies. Run `dbt deps` to install into dbt_packages/.
# dbt_utils provides extra generic tests/macros (accepted_range, etc.).
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
